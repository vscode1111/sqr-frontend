import { makeStyles } from 'tss-react/mui';

export const useSharesPageStyles = makeStyles()(() => ({
  root: {
    display: 'flex',
    flexDirection: 'column',
    gap: 21,
    marginTop: 30,
  },
}));
