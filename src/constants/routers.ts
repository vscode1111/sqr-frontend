export enum ROUTE {
  LOGOUT = 'logout',
  TOKEN = 'token',
  CLAIM = 'claim',
  LOCKUP = 'lockup',
  STAKING = 'staking',
  SIGNATURE = 'signature',
  LAUNCHPAD = 'launchpad',
  PROFILE = 'profile',
  GENERATE_SHARES = 'generate-shares',
  VALIDATORS = 'validators',
}

export enum SUB_ROUTE {
  SHARES = 'shares',
  MONITORING = 'monitoring',
  CONTRACTS = 'contracts',
}
