import { makeStyles } from 'tss-react/mui';

export const useSignaturePageStyles = makeStyles()(() => ({
  root: {
    display: 'flex',
    flexDirection: 'column',
    gap: 21,
    marginTop: 50,
  },
}));
